#!/command/with-contenv sh
# Serves the UI bundle and proxies /api/ to the management API, so the browser only ever talks to
# one origin. The config is generated by `init-web` from the environment.
exec 2>&1

exec nginx -c /etc/nginx/nginx.conf -g 'daemon off;'
