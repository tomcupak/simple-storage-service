# The build stage runs its own `npm ci` and `npm run build`; shipping the host's copies would
# make the context gigabytes large and risk baking a stale or platform-wrong tree into the image.
node_modules
dist
coverage
data

.git
.env
*.log
